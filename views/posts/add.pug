extends ../layout

block content
 form(method="POST")
  label(for="title") Title:
  input(type="text" id="title" name="title")
  label(for="des") Description:
  textarea(id="desc" name="body" rows="10") 
  input(type="submit" value="Add Post")